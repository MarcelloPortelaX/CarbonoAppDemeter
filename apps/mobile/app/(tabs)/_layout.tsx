import { MaterialCommunityIcons } from '@expo/vector-icons';
import { Tabs } from 'expo-router';
import { useDemeterTheme } from '../../src/theme/ThemeProvider';
const items={index:['home','Início'],areas:['leaf','Áreas'],passports:['clipboard-text-outline','Passaportes'],alerts:['bell-outline','Alertas'],profile:['account-outline','Perfil']} as const;
export default function TabsLayout(){console.log('BOOT: tabs');const{theme}=useDemeterTheme();return <Tabs screenOptions={({route})=>({headerShown:false,tabBarActiveTintColor:theme.brand.neon,tabBarInactiveTintColor:theme.colors.textMuted,tabBarStyle:{height:72,paddingTop:8,paddingBottom:8,backgroundColor:theme.colors.surface,borderTopColor:theme.colors.border},tabBarLabelStyle:{fontFamily:'Inter_500Medium',fontSize:10},tabBarIcon:({color,size})=><MaterialCommunityIcons name={items[route.name as keyof typeof items]?.[0]??'circle-outline'} size={size} color={color}/>})}>{Object.entries(items).map(([name,[,label]])=><Tabs.Screen key={name} name={name} options={{title:label}}/>)}</Tabs>}
