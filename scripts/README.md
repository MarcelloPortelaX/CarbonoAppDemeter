# Scripts

- `setup-windows.bat`: instala API e mobile, alinha Expo e roda validações.
- `start-backend.bat`: inicia FastAPI em `0.0.0.0:8000`.
- `start-mobile.bat`: inicia Expo limpando cache.
- `validate-all.bat`: testes, lint, typecheck, Expo Doctor e schemas.
- `build-apk.bat`: valida e dispara EAS Build `preview` em APK.
- `visual_diff.py`: compara screenshots do app aos recortes de referência.
- `validate-project-files.py`: valida JSON/YAML.
